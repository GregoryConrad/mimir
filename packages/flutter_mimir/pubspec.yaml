name: flutter_mimir
description: ⚡ Supercharged Flutter Database
version: 0.0.0
homepage: https://github.com/GregoryConrad/mimir
repository: https://github.com/GregoryConrad/mimir

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mimir: ^0.0.0
  path: ^1.8.2
  path_provider: ^2.0.11

dev_dependencies:
  analyzer: ^5.2.0
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      ios:
        ffiPlugin: true
      macos:
        ffiPlugin: true
      android:
        ffiPlugin: true
      # linux:
      #   ffiPlugin: true
      # windows:
      #   ffiPlugin: true
