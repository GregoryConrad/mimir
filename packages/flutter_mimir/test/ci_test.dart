import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Dummy test for ci', () {
    // At least as of now, no true easily-testable features are in
    // flutter_mimir. It is mostly just ffi wrappers/convenience.
    // Thus, we leave testing up to mimir and the flutter_example.
    expect(true, true);
  });
}
